# 项目协作约定

## 长期记录

- 用户要求记住的信息写入合适的项目文档。
- `AGENTS.md` 只记录通用协作规则；项目事实写入 README、设计文档或审计文档。

## 工作边界

- 新建任何文件（包括脚本、源码、文档、资源和测试文件）前，必须先获得用户明确同意；未经同意不得创建新文件（除非拆分代码文件）。
- 禁止使用 Computer Use 操作本机或应用界面；界面验证仅使用项目既有脚本、截图和命令行流程。
- 不修改服务端。
- 构建后默认直接装机。
- 禁止一切 generic iOS Debug 构建；编译、装机和启动统一使用 `Scripts/build-install-device.sh` 的真机流程。
- 禁止私自运行任何 Xcode 裸命令；必备 Xcode 流程均已集成在项目脚本中，按既有脚本执行。
- 已有脚本和文档流程明确时直接执行。失败、设备变化或文档不一致时再查看脚本内容。

## 表达与输出

- 交流直接，禁用转折句式和否定句式，例如禁止“不是...，而是...“；“只...，不...“。此限制应用在所有范围，包括单不限于对话、文档和代码注释。
- 禁止过度设计和兜底，不做不必要的前后检查。
- 开发过程只保留必要信息；工具与脚本默认尽量静默，发现无用输出立即优化。
- 执行脚本前禁止读取脚本内容；按既有流程直接运行，运行结果再按需处理。
- `git diff`、`git diff --stat` 和 `git status --short` 不作为默认流程；仅在用户明确要求，或编辑、清理、提交、推送确有安全必要时，以最小范围执行。
- 能用默认值满足要求时，不要设置任意自定义值，避免不同区域混乱并保持设计语言统一。
- 必须使用 `Shared/DesignSystem/AppDesignSystem.swift` 的设计令牌和公共组件。
- 已移除的交互不得以旧文案、旧菜单、旧服务或兼容入口重新引入；删除功能后同步清理调用链和说明，不应该出现“此前xxx现已移除“的类似表达。
- 除非用户明确指定，禁止主动运行测试；网络 smoke、扩展测试和静态审计均不默认执行。
- 构建、测试、截图和临时结果按类别各自使用唯一固定路径并覆盖写入；同一性质只能保留一份，不得创建第二个平行目录或文件。文件名使用稳定的类别名，不加 `latest`、`new`、设备名、时间、UUID、序号等修饰语；例如截图只能写 `.build/screenshot.png`。
