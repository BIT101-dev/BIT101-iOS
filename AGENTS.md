# 项目协作约定

## 长期记录

- 用户要求记住的信息写入合适的项目文档。
- `AGENTS.md` 只记录通用协作规则；项目事实写入 README、设计文档或审计文档。
- 固化前避免重复或冲突，完成后告知写入位置。

## 工作边界

- 先确认服务端维护边界。未经用户明确授权，不修改服务端、不提 issue。
- 真机可用时，构建成功后默认直接装机。
- 未经用户明确允许，不执行 `xcodebuild archive` 或 Xcode Archive。
- 已有脚本和文档流程明确时直接执行。失败、设备变化或文档不一致时再排查。

## 表达与输出

- 交流尽量直接，少用转折句式。
- 开发过程只保留必要信息；工具与脚本默认尽量静默，发现无用输出立即削减。
- 禁止过度设计和兜底，不做不必要的前后检查。
- 能用默认值满足要求时，不要设置任意自定义值，避免不同区域混乱并保持设计语言统一。
- 默认先给简洁结论，再补充必要依据。
- 除非用户明确指定，禁止主动运行测试；网络 smoke、扩展测试和静态审计均不默认执行。

- 执行脚本前禁止读取脚本内容；按既有流程直接运行，运行结果再按需处理。
- 构建、测试、截图和临时结果统一使用固定路径覆盖；禁止按时间、UUID或序号无限制新建同类文件。`.build`只保留最新截图与错误报告批次，其余产物可直接清理。
