# 项目协作约定

## 长期记录

- 用户要求记住的信息写入合适的项目文档。
- `AGENTS.md` 只记录通用协作规则；项目事实写入 README、设计文档或审计文档。
- 固化前避免重复或冲突，完成后告知写入位置。

## 工作边界

- 先确认服务端维护边界。未经用户明确授权，不修改服务端、不提 issue。
- 真机可用时，构建成功后默认直接装机。
- 未经用户明确允许，不执行 `xcodebuild archive` 或 Xcode Archive。
- 代码改动后默认只构建并装机，不主动运行测试；仅在用户明确要求时运行测试。
- 已有脚本和文档流程明确时直接执行。失败、设备变化或文档不一致时再排查。

## 表达与输出

- 交流尽量直接，少用转折句式。
- 开发过程只保留必要信息；工具与脚本默认尽量静默，发现无用输出立即削减。
- 禁止过度设计和兜底，不做不必要的前后检查。
- 能用默认值满足要求时，不要设置任意自定义值，避免不同区域混乱并保持设计语言统一。
- UI 样式优先使用 `Shared/DesignSystem/AppDesignSystem.swift` 的设计令牌和公共组件。
- 选择类控件、自绘勾选行和全选/全不选入口统一使用 `Shared/DesignSystem/AppHapticFeedback.swift`；不得在页面直接调用其他触感 API。
- 相同 UI 必须复用公共组件；页面差异通过明确的语义参数或变体表达，不复制后局部微调。
- 右下角圆形操作组和分组列表必须使用设计系统提供的公共组件或修饰器。
- 详情页、信息流、评论区等同类结构必须共享组件；新增入口先扩展公共组件，不另起一套局部实现。
- 颜色、圆角、固定间距和操作按钮尺寸统一走语义令牌；系统默认值可以满足时不新增自定义值。
- 已移除的交互不得以旧文案、旧菜单、旧服务或兼容入口重新引入；删除功能后同步清理调用链和说明。
- 主 App 不直接写调试输出；诊断信息走既有错误报告链路，网络 smoke 输出仅保留在 smoke 实现内。
- 业务网络请求必须经 `HTTPClient` 或场景化 Service；纯逻辑新增或修改时同步补充对应单元测试。
- UI 改动按需运行 `Scripts/check-ui-consistency.sh`，由脚本检查设计令牌和公共组件的使用情况。
- 默认先给简洁结论，再补充必要依据。
- 除非用户明确指定，禁止主动运行测试；网络 smoke、扩展测试和静态审计均不默认执行。

- 执行脚本前禁止读取脚本内容；按既有流程直接运行，运行结果再按需处理。
- 构建、测试、截图和临时结果统一使用固定路径覆盖；禁止按时间、UUID或序号无限制新建同类文件。`.build`只保留最新截图与错误报告批次，其余产物可直接清理。
