# 项目协作约定

## 长期记录约定

- 当用户要求“记住”某项信息时，不应只在当前对话中确认；应将其固化到适合长期维护的项目文档中。
- `AGENTS.md` 只记录如何协作、如何维护文档等通用规则；具体项目事实、资源归属、功能或操作说明应写入对应的 README、设计文档或审计文档。
- `AGENTS.md` 应保持简洁，只保留长期、通用且必要的协作原则，不记录具体任务或操作层面的细节。
- 固化前避免重复或冲突；完成后告知用户写入了哪个文件以及记录内容。
- 涉及服务端能力时，先确认项目维护边界，不默认客户端维护者拥有服务端修改权限。
- 未经用户明确授权，不修改服务端、不提出服务端改动预期，也不代为提交服务端 issue。
- 真机可用时，构建成功后默认直接装机，不再另行询问。
- 已有脚本和文档流程明确时直接执行，不重复核查；仅在失败、设备变化或实际与文档不一致时排查。
- 交流尽量直接，少用转折句式。
- 禁止过度设计和兜底；运行工具前不做不必要检查或确认，工具明确成功后不重复复核。

- 默认少说结论，多用代码和检查结果推进；退出前务必多跑几次关键代码/测试再收尾。
